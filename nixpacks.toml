# Nixpacks configuration for Railway deployment
# Explicitly use Python provider only (no Django auto-detection)

[[providers]]
name = "python"

[build]
# Use the build.sh script
cmds = ["bash build.sh"]

[start]
# Use the railway_start.sh script
cmd = "bash deploy/railway_start.sh"
